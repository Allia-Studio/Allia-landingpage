---
interface Props {
  class?: string;
  as?: 'div' | 'section' | 'article' | 'main';
}

const {
  class: className,
  as: Tag = 'div'
} = Astro.props;

const classes = `w-full max-w-[1800px] mx-auto ${className || ''}`.trim();
---

<Tag class={classes}>
  <slot />
</Tag>
